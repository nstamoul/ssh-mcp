# MCP SSH HTTP Server Configuration

# Port to listen on (default: 3009)
PORT=3009

# Host to bind to (default: 0.0.0.0)
# Use 127.0.0.1 for localhost only, or 0.0.0.0 to listen on all interfaces
HOST=0.0.0.0

# Enable debug logging (default: false)
DEBUG=false
